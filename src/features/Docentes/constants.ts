/** Nombre del query param de búsqueda en la URL (spec §3: filtros enlazables vía `useSearchParams`). */
export const DOCENTE_QUERY_PARAM = 'q'

/** Espera tras la última tecla antes de escribir el término en la URL, para no navegar en cada pulsación. */
export const DOCENTE_SEARCH_DEBOUNCE_MS = 300
